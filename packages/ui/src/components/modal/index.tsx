import { ModalBackground, ModalContainer } from './styles';

const Modal = () => {
    return (
        <ModalBackground>
            <ModalContainer>
                <div>x</div>
            </ModalContainer>
        </ModalBackground>
    );
};

export default Modal;
